# :memo: CSV Data Sort

CSV file reading and sorting algorithms in Python and C++ with Tkinter GUI.

## Generate CSV file

You can use `csv_generator.py` script both in command line mode and from the GUI.

### Command line mode usage example

```bash
python3 csv_generator.py --path ~/.cache/csvdatasort/data.csv
```